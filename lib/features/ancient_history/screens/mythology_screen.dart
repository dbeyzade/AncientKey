import 'package:flutter/material.dart';

class MythologyScreen extends StatelessWidget {
  const MythologyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('mythology')),
      body: const Center(child: Text('Geliştirme devam ediyor...')),
    );
  }
}

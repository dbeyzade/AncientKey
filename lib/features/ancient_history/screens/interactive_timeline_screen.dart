import 'package:flutter/material.dart';

class InteractiveTimelineScreen extends StatelessWidget {
  const InteractiveTimelineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('⏳ Tarih Zaman Çizelgesi')),
      body: const Center(child: Text('Timeline ekranı - Geliştirme devam ediyor')),
    );
  }
}

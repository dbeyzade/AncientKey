import 'package:flutter/material.dart';

class TradeRoutesScreen extends StatelessWidget {
  const TradeRoutesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('trade routes')),
      body: const Center(child: Text('Geliştirme devam ediyor...')),
    );
  }
}

import 'package:flutter/material.dart';

class AncientWarsScreen extends StatelessWidget {
  const AncientWarsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ancient wars')),
      body: const Center(child: Text('Geliştirme devam ediyor...')),
    );
  }
}
